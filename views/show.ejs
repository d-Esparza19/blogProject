<%- include("partials/header") %>

<div class="ui main text container segment">
        <div class="ui huge header"> <%= blog.title %> </div>
        <div class="ui top attached ">
            <div class="item">
                <img class="ui centered rounded image" src="<%= blog.image %>"></div>
                <div class="content">
                    <span><%= blog.created.toDateString() %></span>
                </div>
                <div class="description">        
                    <!-- // need to sanitize this - will evaluate html currently -->
                    <p><%- blog.body %></p>
                </div>
                <a class="ui basic orange basic button" href="/blogs/<%= blog._id %>/edit">Edit</a>
        </div>
        
</div>

<%- include("partials/footer") %>